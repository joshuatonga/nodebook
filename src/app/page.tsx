import { NodebookAppLoader } from "@/components/nodebook-app-loader";

export default function HomePage() {
  return (
    <main className="server-shell">
      <NodebookAppLoader />
    </main>
  );
}
